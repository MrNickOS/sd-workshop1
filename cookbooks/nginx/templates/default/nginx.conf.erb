worker_processes 1;
 events {
    worker_connections 2048;
 }

http {
  upstream servers {
    server 192.168.60.100;
    server 192.168.60.110;
  }

  server {
    listen 8080;
    location / {
      proxy_pass http://servers;
    }
  }
}
